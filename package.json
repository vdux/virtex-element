{
  "name": "virtex-element",
  "description": "A slightly opinionated, higher-level JSX element creator for virtex",
  "repository": "git://github.com/ashaffer/virtex-element.git",
  "version": "0.1.3",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "dependencies": {
    "ev-store": "^7.0.0",
    "foreach": "github:micro-js/foreach",
    "to-style": "^1.3.3"
  },
  "peerDependencies": {
    "virtex": "*"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
